{
  "name": "book-editor",
  "version": "1.0.0",
  "description": "Professional Book Editor with AI-powered editing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:frontend": "cd frontend && npm install && npm run build && cp -r dist/* ../public/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "docx": "^8.2.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
